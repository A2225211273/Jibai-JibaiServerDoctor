# 漏斗密集检测

漏斗数量过多会增加服务器负担。JibaiOptimizer 会后台分批扫描已加载区块中的漏斗数量。

## 分批扫描

插件不会在执行 `/optimizer hoppers` 时即时全量扫描，而是后台按 tick 分批处理区块。

## 缓存结果

`/optimizer hoppers` 只读取最近一次扫描缓存。如果第一轮扫描尚未完成，会提示稍后再试。

## 安全说明

插件只提醒漏斗密集区块，不破坏漏斗。
